# Example environment variables for HSR Tech frontend (Vite)
# Copy this file to `.env` or set these variables in your environment / CI.
# Vite environment variables MUST be prefixed with VITE_

# Backend API base URL used by the frontend. Change this to your API host.
# Example (development):
VITE_API_URL=http://localhost:4500

# Example (production):
# VITE_API_URL=https://api.example.com
